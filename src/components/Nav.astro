---
// Navigation component with responsive design
const currentPath = Astro.url.pathname;

const navItems = [
  { href: '/', label: 'Home' },
  { href: '/blog', label: 'Blog' },
  { href: '/daily-verses', label: 'Daily Verses' },
  { href: '/resources', label: 'Resources' },
  { href: '/about', label: 'About' },
  { href: '/contact', label: 'Contact' },
];

const isActive = (href: string) => {
  if (href === '/daily-verses') {
    return currentPath === href || currentPath.startsWith('/daily-verses/');
  }
  if (href === '/resources') {
    return currentPath === href || currentPath.startsWith('/resources/');
  }
  if (href === '/blog') {
    return currentPath === href || currentPath.startsWith('/blog/');
  }
  return currentPath === href;
};
---

<nav class="absolute top-0 left-0 right-0 z-50 bg-transparent" role="navigation" aria-label="Main navigation">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16 md:h-20">
      <!-- Site name on the left -->
      <div class="flex items-center">
        <a href="/" class="text-lg md:text-xl font-bold text-white hover:text-gray-200 transition-colors" aria-label="How God Speaks To Us - Home">
          How God Speaks To Us
        </a>
      </div>
      
      <!-- Desktop navigation on the right -->
      <div class="hidden md:flex items-center space-x-6 lg:space-x-8">
        {navItems.map((item) => (
          <a
            href={item.href}
            class={`text-sm md:text-base font-medium transition-colors ${
              isActive(item.href)
                ? 'text-white border-b-2 border-white pb-1'
                : 'text-gray-300 hover:text-white'
            }`}
            aria-current={isActive(item.href) ? 'page' : undefined}
          >
            {item.label.toUpperCase()}
          </a>
        ))}
      </div>

      <!-- Mobile menu button -->
      <div class="md:hidden flex items-center">
        <button
          type="button"
          class="inline-flex items-center justify-center p-2 rounded-md text-white hover:text-gray-200 hover:bg-white hover:bg-opacity-10 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white"
          aria-controls="mobile-menu"
          aria-expanded="false"
          id="mobile-menu-button"
        >
          <span class="sr-only" id="mobile-menu-label">Open main menu</span>
          <!-- Icon when menu is closed -->
          <svg
            class="block h-6 w-6 menu-icon-open"
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
            aria-hidden="true"
          >
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
          <!-- Icon when menu is open -->
          <svg
            class="hidden h-6 w-6 menu-icon-close"
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
            aria-hidden="true"
          >
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile menu -->
  <div class="md:hidden hidden bg-black bg-opacity-95" id="mobile-menu">
    <div class="pt-2 pb-3 space-y-1 px-4">
      {navItems.map((item) => (
        <a
          href={item.href}
          class={`block px-3 py-2 text-base font-medium transition-colors ${
            isActive(item.href)
              ? 'text-white border-l-4 border-white bg-white bg-opacity-10'
              : 'text-gray-300 hover:text-white hover:bg-white hover:bg-opacity-10'
          }`}
          aria-current={isActive(item.href) ? 'page' : undefined}
        >
          {item.label.toUpperCase()}
        </a>
      ))}
    </div>
  </div>
</nav>

<script>
  const mobileMenuButton = document.getElementById('mobile-menu-button');
  const mobileMenu = document.getElementById('mobile-menu');
  const mobileMenuLabel = document.getElementById('mobile-menu-label');
  const menuIconOpen = mobileMenuButton?.querySelector('.menu-icon-open');
  const menuIconClose = mobileMenuButton?.querySelector('.menu-icon-close');

  if (mobileMenuButton && mobileMenu) {
    mobileMenuButton.addEventListener('click', () => {
      const isExpanded = mobileMenuButton.getAttribute('aria-expanded') === 'true';
      const nextExpanded = !isExpanded;

      mobileMenuButton.setAttribute('aria-expanded', nextExpanded.toString());

      if (mobileMenuLabel) {
        mobileMenuLabel.textContent = nextExpanded ? 'Close main menu' : 'Open main menu';
      }

      menuIconOpen?.classList.toggle('hidden', nextExpanded);
      menuIconOpen?.classList.toggle('block', !nextExpanded);
      menuIconClose?.classList.toggle('hidden', !nextExpanded);
      menuIconClose?.classList.toggle('block', nextExpanded);

      if (isExpanded) {
        mobileMenu.classList.add('hidden');
      } else {
        mobileMenu.classList.remove('hidden');
      }
    });
  }
</script>
